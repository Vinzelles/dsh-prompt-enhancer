# CONTEXT

`@dsh-external/dsh-prompt-enhancer`(提示词增强插件)的领域术语表。本文件只收录领域语言,不含实现细节(实现决策见 `docs/adr/`,代码即实现记录)。

## 术语

- **强化(Enhance)**:将用户的原始提示词重写为结构清晰、约束明确、适合直接交给 agent 执行的任务提示词,并(在存在工作目录且检索命中时)附上参考文件清单的过程。执行通道为隔离子会话 agent(失败时降级为同模型直调),结果只替换输入框草稿,不进入主对话历史。
- **原始提示词(Original Prompt)**:用户在手写输入框中输入的文本,是强化的唯一输入。
- **增强提示词(Enhanced Prompt)**:强化的产物。保持输入语言;不增删用户真实需求;允许追加最少必要的工作指令;末尾可有「参考文件」节;原始提示词中的技能引用记号必须全部原样保留。
- **参考文件清单(File Reference Block)**:附加在增强提示词末尾的小节,每行「相对路径 — 一句话相关性说明」,最多 5 个文件;对 ≤2 个高相关短文件可附 ≤20 行摘录。无工作目录或零命中时整节省略。
- **技能引用(Skill Reference)**:原始提示词中 `/skill-name` 形式的宿主技能调用记号——发送后由宿主识别并向对话注入该技能的完整说明。它不是普通文字,任何改写、翻译或转述都会让引用失效;强化必须让每个记号出现在增强提示词中(融入正文或集中为「技能引用」节)。

## 关系

- 增强提示词 = 强化(原始提示词, 增强上下文),可含参考文件清单与技能引用节。
- 引用不丢:记号集(增强提示词) ⊇ 引用集(原始提示词);模型改写致缺失时由宿主以「技能引用」节确定性补齐。
- 还原 = 原始提示词 ↺ 增强提示词(单级、严格触发:草稿 === 增强文本时可见)。
- 强化不自动发送:增强提示词只替换草稿,由用户审阅后手动发送。
- 仅本机(Loopback-only):三个 HTTP 端点只接受本机回环来源(`127.0.0.1`/`::1`/`::ffff:127.0.0.1`);非本机来源立即 403——服务暴露时强化端点不得成为外部消耗 LLM 额度与窥探仓库的入口(fail-closed:缺失来源信息按非本机处理)。
